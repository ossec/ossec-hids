FROM fedora:43

RUN dnf -y update && \
    dnf -y install \
    gcc \
    make \
    openssl-devel \
    pcre2-devel \
    zlib-devel \
    systemd-devel \
    findutils \
    which \
    && \
    dnf clean all

WORKDIR /src
